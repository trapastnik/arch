diff --git a/index.html b/index.html
index 8b1f1f1..0000000 100644
--- a/index.html
+++ b/index.html
@@ -14,6 +14,7 @@
   <div class="wrap">
     <h1>📷 Распознавание планшетов (ONNXRuntime Web)</h1>
 
     <div class="card">
       <div class="video-wrap">
         <video id="video" playsinline muted></video>
         <canvas id="overlay"></canvas>
       </div>
 
       <div class="controls">
         <button id="btnStart">Старт камеры</button>
         <button id="btnStop" disabled>Стоп</button>
         <button id="btnFlip">Поменять камеру</button>
-        <label class="switch"><input id="chkAutoBox" type="checkbox" checked /> Авто-рамка</label>
       </div>
 
+      <!-- 🔧 Настройки (живые) -->
+      <div class="settings">
+        <div class="row">
+          <label>Кадрирование:</label>
+          <select id="selCrop">
+            <option value="full">Full frame (resize)</option>
+            <option value="center">Center-crop (square)</option>
+            <option value="auto" selected>Auto-ROI (детектор)</option>
+          </select>
+        </div>
+        <div class="row">
+          <label>Нормализация:</label>
+          <select id="selNorm">
+            <option value="neg1to1" selected>[-1, 1]</option>
+            <option value="zero1">[0, 1]</option>
+            <option value="imagenet">ImageNet mean/std</option>
+          </select>
+        </div>
+        <div class="row">
+          <label>ROI pad: <span id="valPad">0.08</span></label>
+          <input id="rngPad" type="range" min="0" max="0.30" step="0.01" value="0.08" />
+        </div>
+        <div class="row">
+          <label>EMA: <span id="valEma">0.35</span></label>
+          <input id="rngEma" type="range" min="0" max="0.95" step="0.01" value="0.35" />
+        </div>
+      </div>
+
       <div id="result">Готово к запуску…</div>
       <div id="sub" class="muted">Загрузится модель и карта классов.</div>
 
diff --git a/styles.css b/styles.css
index 2a2a2a2..0000000 100644
--- a/styles.css
+++ b/styles.css
@@ -45,6 +45,18 @@ pre {
 }
 .footer { opacity:.6; font-size:11.5px; margin-top:8px; }
 
+/* 🔧 settings */
+.settings { display:grid; grid-template-columns: 1fr; gap:8px; margin:8px 0 6px; }
+.settings .row { display:flex; align-items:center; gap:8px; }
+.settings label { min-width:130px; font-size:13px; opacity:.9; }
+.settings select, .settings input[type="range"]{
+  flex:1;
+  background:#222a36;
+  color:#fff;
+  border:1px solid #2b3140;
+  border-radius:8px;
+  padding:6px 8px;
+}
 @media (max-width: 390px) {
   #result { font-size: 15px; }
   button { font-size: 13px; padding: 7px 10px; }
diff --git a/src/constants.js b/src/constants.js
index 1111111..0000000 100644
--- a/src/constants.js
+++ b/src/constants.js
@@ -1,8 +1,16 @@
 export const MODEL_URL = 'web_model/model.onnx';
 export const MAP_URL   = 'class_mapping.json';
 
 export const INPUT_SIZE = 224;        // размер входа модели
 export const LOOP_INTERVAL_MS = 150;  // частота инференса
 export const EDGE_SIZE = 160;         // масштаб для быстрого поиска рамки
+
+// дефолтные настройки (меняются в рантайме через UI)
+export const DEFAULTS = {
+  cropMode: 'auto',     // 'full' | 'center' | 'auto'
+  normMode: 'neg1to1',  // 'neg1to1' | 'zero1' | 'imagenet'
+  roiPad: 0.08,
+  boxEma: 0.35
+};
diff --git a/src/preprocess.js b/src/preprocess.js
index 2222222..0000000 100644
--- a/src/preprocess.js
+++ b/src/preprocess.js
@@ -1,28 +1,71 @@
 import { INPUT_SIZE } from './constants.js';
 
-export function preprocessToTensor(videoEl, lastBox, useNCHW, overlay){
-  const W = INPUT_SIZE, H = INPUT_SIZE;
-  const cvs = document.createElement('canvas');
-  cvs.width = W; cvs.height = H;
-  const ctx = cvs.getContext('2d', { willReadFrequently: true });
-
-  if (lastBox && overlay){
-    const dpr = devicePixelRatio;
-    const sx = lastBox.x / dpr, sy = lastBox.y / dpr;
-    const sw = lastBox.w / dpr, sh = lastBox.h / dpr;
-    ctx.drawImage(videoEl, sx, sy, sw, sh, 0, 0, W, H);
-  } else {
-    ctx.drawImage(videoEl, 0, 0, W, H);
-  }
-
-  const { data } = ctx.getImageData(0,0,W,H);
-  const wh = W*H;
-
-  if (useNCHW) {
-    const floats = new Float32Array(wh*3);
-    for (let i=0,p=0;i<wh;i++){ const r=data[p++],g=data[p++],b=data[p++]; p++; floats[i]=(r/127.5-1); floats[i+wh]=(g/127.5-1); floats[i+2*wh]=(b/127.5-1); }
-    return new ort.Tensor('float32', floats, [1,3,H,W]);
-  } else {
-    const floats = new Float32Array(wh*3);
-    for (let i=0,p=0,q=0;i<wh;i++){ const r=data[p++],g=data[p++],b=data[p++]; p++; floats[q++]=(r/127.5-1); floats[q++]=(g/127.5-1); floats[q++]=(b/127.5-1); }
-    return new ort.Tensor('float32', floats, [1,H,W,3]);
-  }
-}
+// нормализация по выбранному режиму
+function normalizePixel(r,g,b, normMode){
+  if (normMode === 'zero1')    return [r/255, g/255, b/255];
+  if (normMode === 'imagenet') {
+    const mean=[123.675,116.28,103.53], std=[58.395,57.12,57.375];
+    return [(r-mean[0])/std[0], (g-mean[1])/std[1], (b-mean[2])/std[2]];
+  }
+  // default neg1to1
+  return [(r/127.5-1), (g/127.5-1), (b/127.5-1)];
+}
+
+export function preprocessToTensor({
+  videoEl, overlay, cropMode, lastBox, roiPad, useNCHW, normMode
+}){
+  const W = INPUT_SIZE, H = INPUT_SIZE;
+  const cvs = document.createElement('canvas');
+  cvs.width = W; cvs.height = H;
+  const ctx = cvs.getContext('2d', { willReadFrequently: true });
+
+  if (cropMode === 'full' || !overlay){
+    // просто ресайз
+    ctx.drawImage(videoEl, 0, 0, W, H);
+  } else if (cropMode === 'center') {
+    // центр-кроп квадрат
+    const vw = videoEl.videoWidth || overlay.width;
+    const vh = videoEl.videoHeight || overlay.height;
+    const side = Math.min(vw, vh);
+    const sx = Math.floor((vw - side)/2);
+    const sy = Math.floor((vh - side)/2);
+    ctx.drawImage(videoEl, sx, sy, side, side, 0, 0, W, H);
+  } else {
+    // auto-roi (квадрат + паддинг)
+    const dpr = devicePixelRatio || 1;
+    const cx = (lastBox?.x ?? 0 + (lastBox?.w ?? overlay.width)*0.5)/dpr;
+    const cy = (lastBox?.y ?? 0 + (lastBox?.h ?? overlay.height)*0.5)/dpr;
+    const s  = Math.max(lastBox?.w ?? overlay.width, lastBox?.h ?? overlay.height)/dpr;
+    const side = Math.max(16, Math.floor(s + 2*(s*roiPad)));
+    const sx = Math.max(0, Math.floor(cx - side/2));
+    const sy = Math.max(0, Math.floor(cy - side/2));
+    ctx.drawImage(videoEl, sx, sy, side, side, 0, 0, W, H);
+  }
+
+  const { data } = ctx.getImageData(0,0,W,H);
+  const wh = W*H;
+
+  if (useNCHW) {
+    const floats = new Float32Array(wh*3);
+    for (let i=0,p=0;i<wh;i++){
+      const r=data[p++], g=data[p++], b=data[p++]; p++;
+      const [nr,ng,nb] = normalizePixel(r,g,b,normMode);
+      floats[i]=nr; floats[i+wh]=ng; floats[i+2*wh]=nb;
+    }
+    return new ort.Tensor('float32', floats, [1,3,H,W]);
+  } else {
+    const floats = new Float32Array(wh*3);
+    for (let i=0,p=0,q=0;i<wh;i++){
+      const r=data[p++], g=data[p++], b=data[p++]; p++;
+      const [nr,ng,nb] = normalizePixel(r,g,b,normMode);
+      floats[q++]=nr; floats[q++]=ng; floats[q++]=nb;
+    }
+    return new ort.Tensor('float32', floats, [1,H,W,3]);
+  }
+}
diff --git a/src/main.js b/src/main.js
index 3333333..0000000 100644
--- a/src/main.js
+++ b/src/main.js
@@ -1,20 +1,38 @@
-import { INPUT_SIZE, LOOP_INTERVAL_MS } from './constants.js';
+import { INPUT_SIZE, LOOP_INTERVAL_MS, DEFAULTS } from './constants.js';
 import { $, setText, setStatus, logDiag, resizeOverlay } from './ui.js';
 import { startCamera, stopCamera, hasStream } from './camera.js';
 import { loadModel } from './model.js';
 import { findTabletBox, drawBox } from './box.js';
-import { preprocessToTensor, topkSoftmax } from './preprocess.js';
+import { preprocessToTensor, topkSoftmax } from './preprocess.js';
 
 let state = {
   session: null,
   INPUT_NAME: null,
   OUTPUT_NAME: null,
   useNCHW: false,
   mapping: null,
   usingBack: true,
-  autoBox: true,
+  // живые настройки
+  cropMode: DEFAULTS.cropMode,   // 'full' | 'center' | 'auto'
+  normMode: DEFAULTS.normMode,   // 'neg1to1' | 'zero1' | 'imagenet'
+  roiPad:   DEFAULTS.roiPad,
+  boxEma:   DEFAULTS.boxEma,
   lastBox: null,
   rafId: null,
   lastTs: 0,
   video: null,
   overlay: null,
   octx: null,
 };
 
+function bindSettingsUI(){
+  const selCrop = $('#selCrop');
+  const selNorm = $('#selNorm');
+  const rngPad  = $('#rngPad');
+  const rngEma  = $('#rngEma');
+  const valPad  = $('#valPad');
+  const valEma  = $('#valEma');
+  if (!selCrop) return; // если старый index.html
+  selCrop.value = state.cropMode;
+  selNorm.value = state.normMode;
+  rngPad.value  = String(state.roiPad);
+  rngEma.value  = String(state.boxEma);
+  valPad.textContent = state.roiPad.toFixed(2);
+  valEma.textContent = state.boxEma.toFixed(2);
+  selCrop.addEventListener('change', ()=>{ state.cropMode = selCrop.value; });
+  selNorm.addEventListener('change', ()=>{ state.normMode = selNorm.value; });
+  rngPad.addEventListener('input', ()=>{ state.roiPad = +rngPad.value; valPad.textContent = state.roiPad.toFixed(2); });
+  rngEma.addEventListener('input', ()=>{ state.boxEma = +rngEma.value; valEma.textContent = state.boxEma.toFixed(2); });
+}
+
 async function onStartClick(){
   try{
     $('#btnStart').disabled = true;
@@ -24,11 +42,15 @@
     Object.assign(state, model);
 
-    state.video = await startCamera(state.usingBack);
+    bindSettingsUI();
+
+    state.video = await startCamera(state.usingBack);
     state.overlay = document.getElementById('overlay');
     state.octx = state.overlay.getContext('2d');
 
     resizeOverlay(state.video, state.overlay);
-    window.addEventListener('resize', onResize, { passive:true });
+    window.addEventListener('resize', onResize, { passive:true });
 
     $('#btnStop').disabled = false;
     setStatus('Камера запущена. Распознаём…', null);
     state.rafId = requestAnimationFrame(loop);
   }catch(e){
@@ -53,19 +75,33 @@
   if (!state.lastTs || ts - state.lastTs >= LOOP_INTERVAL_MS) {
     state.lastTs = ts;
     try{
-      // авторамка
-      if (state.autoBox) {
-        state.lastBox = findTabletBox(state.video, state.overlay) || state.lastBox;
-        drawBox(state.octx, state.overlay, state.lastBox);
-      } else {
-        drawBox(state.octx, state.overlay, null);
-      }
+      // авторамка только при cropMode==='auto'
+      if (state.cropMode === 'auto') {
+        const b = findTabletBox(state.video, state.overlay) || state.lastBox;
+        if (b && state.lastBox) {
+          const a = state.boxEma;
+          state.lastBox = {
+            x: Math.round(a*state.lastBox.x + (1-a)*b.x),
+            y: Math.round(a*state.lastBox.y + (1-a)*b.y),
+            w: Math.round(a*state.lastBox.w + (1-a)*b.w),
+            h: Math.round(a*state.lastBox.h + (1-a)*b.h),
+          };
+        } else {
+          state.lastBox = b;
+        }
+        drawBox(state.octx, state.overlay, state.lastBox);
+      } else {
+        drawBox(state.octx, state.overlay, null);
+      }
 
-      // препроцесс + инференс
-      const x = preprocessToTensor(state.video, state.autoBox ? state.lastBox : null, state.useNCHW, state.overlay);
+      // препроцесс + инференс
+      const x = preprocessToTensor({
+        videoEl: state.video,
+        overlay: state.overlay,
+        cropMode: state.cropMode,
+        lastBox: state.lastBox,
+        roiPad: state.roiPad,
+        useNCHW: state.useNCHW,
+        normMode: state.normMode
+      });
       state.session.run({ [state.INPUT_NAME]: x }).then(out => {
         const logits = out[state.OUTPUT_NAME].data;
         const { top1Idx, top1Prob, top3 } = topkSoftmax(logits, 3);
-        const name = state.mapping[String(top1Idx)] ?? `class_${top1Idx}`;
-        setText('result', `🧩 ${name} (${(top1Prob*100).toFixed(1)}%)`);
-        setText('sub', `Класс #${top1Idx} • Формат: ${state.useNCHW?'NCHW':'NHWC'}${state.autoBox?' • ROI: on':''}`);
+        const name = state.mapping[String(top1Idx)] ?? `class_${top1Idx}`;
+        setText('result', `🧩 ${name} (${(top1Prob*100).toFixed(1)}%)`);
+        setText('sub', `#${top1Idx} • ${state.useNCHW?'NCHW':'NHWC'} • ${state.cropMode} • norm:${state.normMode}`);
         document.getElementById('topk').innerHTML = top3.map(
           ({idx, prob}) => `<span class="pill">${state.mapping[String(idx)] ?? 'class_'+idx} — ${(prob*100).toFixed(1)}%</span>`
         ).join('');
@@ -89,7 +125,6 @@
 document.getElementById('btnStart').addEventListener('click', onStartClick);
 document.getElementById('btnStop').addEventListener('click', onStopClick);
 document.getElementById('btnFlip').addEventListener('click', onFlipClick);
-document.getElementById('chkAutoBox')?.addEventListener('change', (e)=>{ state.autoBox = e.target.checked; if(!state.autoBox) drawBox(state.octx, state.overlay, null); });
 
-// начальный подсказочный текст
-setText('sub', `Вход: ${INPUT_SIZE}×${INPUT_SIZE}. Нажми «Старт камеры».`);
+// начальный подсказочный текст
+setText('sub', `Вход: ${INPUT_SIZE}×${INPUT_SIZE}. Выбери режим и жми «Старт».`);